resources:
  jobs:
    valuation_data_ingestion_job:
      name: valuation_data_ingestion
      tags:
        project_name: "valuation-curator"

      environments:
        - environment_key: default
          spec:
            environment_version: "4"
            dependencies:
              - ../dist/*.whl

      tasks:
        - task_key: valuation_data_ingestion
          environment_key: default
          notebook_task:
            notebook_path: ../notebooks/1.3_valuation_data_ingestion.py
            base_parameters:
              env: ${bundle.target}
              git_sha: "${var.git_sha}"
              run_id: "{{job.run_id}}"
